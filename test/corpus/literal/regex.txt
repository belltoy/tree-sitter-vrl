==========================
Regex Literals
==========================

# result: r'foo?bar"baz"\'qux'

r'foo?bar"baz"\'qux'

--------------------------

(program
  (comment)
  (regex
    (regex_escape_sequence)))
